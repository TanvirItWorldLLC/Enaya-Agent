[build-system] requires = [hatchling] build-backend = hatchling.build [project] name = enaya-agent version = 0.1.0 description = A full-stack autonomous agent platform readme = README.md license = MIT requires-python = >=3.10 dependencies = [ pydantic>=2.0, fastapi>=0.110, uvicorn[standard]>=0.29, websockets>=12.0, textual>=0.52, rich>=13.0, httpx>=0.27, sqlalchemy>=2.0, aiosqlite>=0.20, chromadb>=0.4, tiktoken>=0.6, pyyaml>=6.0, python-dotenv>=1.0, click>=8.0, structlog>=24.0, anyio>=4.0, mcp>=1.0, openai>=1.30, anthropic>=0.28, docker>=7.0, paramiko>=3.4, kubernetes>=29.0, ] [project.optional-dependencies] dev = [ pytest>=8.0, pytest-asyncio>=0.23, pytest-cov>=5.0, black>=24.0, ruff>=0.4, mypy>=1.9, ] [project.scripts] enaya = enaya.cli:main [tool.hatch.build.targets.wheel] packages = [enaya] [tool.black] line-length = 100 [tool.ruff] line-length = 100 [tool.mypy] python_version = 3.10 strict = true